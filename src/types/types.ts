export interface userCredentials {
    email: string;
    username: string;
    uid: string;
}

export interface userDetails {
    firstName: string;
    lastName: string;
    phoneNumber: string;
    birthDate: string;
    gender: string;
    address: string;
    country: string;
    city: string;
    title: string;
    description: string;
}
